#include <stdio.h>

/**
 * main - th scores of 10 individuals
 * store in ascending order
 *
 * return: 0
 */
int main()
{
	int score[10];
	int i, j, temp;

	printf("scores of 10 individuals from 1 - :\n ");
		scanf("%d", &score[i]);
                {
                        for(int i = 0; i < 10; i++);
                        {
                                for(j = i + 1; j < 9; j++);
                        }
                }
        return 0;
}
